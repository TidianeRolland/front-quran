import { FilterSurahPipe } from './filter-surah.pipe';

describe('FilterSurahPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSurahPipe();
    expect(pipe).toBeTruthy();
  });
});
